{
  "name": "@graphvault/mcp",
  "version": "0.0.0",
  "private": true,
  "description": "GraphVault MCP server — expose a vault as MCP resources + tools over stdio.",
  "type": "module",
  "bin": {
    "graphvault-mcp": "./dist/index.js"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "dev": "tsc -b --watch",
    "typecheck": "tsc -b",
    "clean": "rm -rf dist *.tsbuildinfo",
    "test": "node --test dist/handlers.test.js"
  },
  "dependencies": {
    "@graphvault/engine": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2"
  }
}
