# @graphvault/sync-core

Reusable sync protocol logic (client agent + server reconciliation helpers).
Scaffolded in Milestone 5. The wire types currently live in `@graphvault/shared`
under `src/sync/`.
